# Demo Environment Configuration
# This file is used for the demo deployment branch

# Database - Demo database (separate from production)
DATABASE_URL=mongodb+srv://demo:demo123@demo-cluster.mongodb.net/biensperience-demo?retryWrites=true&w=majority

# Authentication Secrets (demo-specific)
SECRET=DemoSecretKey2024
SESSION_SECRET=DemoSessionSecret2024
CSRF_SECRET=DemoCsrfSecret2024

# AWS S3 Configuration (use demo bucket)
BUCKET_NAME=biensperience-demo
S3_PROTECTED_BUCKET_NAME=biensperience-demo-private
AWS_REGION=us-west-2
# Note: AWS credentials should be set via environment variables in deployment platform
# ACCESS_KEY_ID=<set in deployment environment>
# SECRET_ACCESS_KEY=<set in deployment environment>

# Language Configuration
REACT_APP_LANG=en

# Demo Mode - Enables demo-specific features
REACT_APP_DEMO_MODE=true

# Debug mode - Disabled for demo
REACT_APP_DEBUG=false

# Disable Fast Refresh for production builds
FAST_REFRESH=false

# OAuth - Disabled for demo (users should use email/password)
# TWITTER_CLIENT_ID=
# TWITTER_CLIENT_SECRET=
# FACEBOOK_APP_ID=
# FACEBOOK_APP_SECRET=

# Email Configuration (demo mode - emails are logged, not sent)
# RESEND_API_KEY=
FROM_EMAIL=demo@biensperience.com
CLIENT_URL=https://demo.biensperience.com

# Demo User Credentials (for reference)
# Regular users: any email from sample data with password "demo123"
# Super admin: check sampleData.txt after running `node sampleData.js`
