/**
 * AlertWrapper - Design System Alert Component
 *
 * Re-exports the alert wrapper component.
 * All consumers should import from design-system for automatic
 * feature-flagged switching between implementations.
 */
export { default } from './AlertWrapper';
export { default as Alert } from './AlertWrapper';
